import { PUBLIC_SITE_ORIGIN } from "./publicQuestions.js";

const escapeHtml = (value: unknown) => String(value ?? "").replace(/[&<>"']/g, (char) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" })[char]!);
const jsonForScript = (value: unknown) => JSON.stringify(value).replace(/</g, "\\u003c").replace(/>/g, "\\u003e").replace(/&/g, "\\u0026");

export function buildPublicQuestionHtml(question: any): string {
  const topic = question.contents[0] || question.subjects[0] || "estudos";
  const seoTitle = `Questão de ${question.discipline} da ${question.institution}${question.year ? ` ${question.year}` : ""} sobre ${topic} | Projeto Vetor`;
  const description = `Resolva uma questão de ${question.discipline} da ${question.institution}${question.year ? ` ${question.year}` : ""} sobre ${topic}, veja o gabarito e confira a resolução comentada no Projeto Vetor.`;
  const canonical = `${PUBLIC_SITE_ORIGIN}/questoes/${question.slug}`;
  const options = question.options.map((option: any) => `<button class="option" data-option="${option.key}" type="button"><strong>${option.key.toUpperCase()}</strong><span>${escapeHtml(option.text)}</span>${option.imageUrl ? `<img src="${escapeHtml(option.imageUrl)}" alt="Imagem da alternativa ${option.key.toUpperCase()}" loading="lazy">` : ""}</button>`).join("");
  const related = question.related.map((item: any) => `<a href="/questoes/${escapeHtml(item.slug)}"><strong>${escapeHtml(item.title)}</strong>${item.topic ? `<span>${escapeHtml(item.topic)}</span>` : ""}</a>`).join("");
  const tags = [...question.contents, ...question.subjects].map((tag: string) => `<span class="tag">${escapeHtml(tag)}</span>`).join("");
  const jsonLd = { "@context": "https://schema.org", "@type": "LearningResource", name: seoTitle, description, url: canonical, inLanguage: "pt-BR", learningResourceType: "Practice problem", educationalLevel: question.difficulty || undefined, about: [question.discipline, ...question.contents, ...question.subjects], provider: { "@type": "Organization", name: "Projeto Vetor", url: PUBLIC_SITE_ORIGIN } };
  return `<!doctype html><html lang="pt-BR"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${escapeHtml(seoTitle)}</title><meta name="description" content="${escapeHtml(description)}"><link rel="canonical" href="${canonical}"><meta name="robots" content="index,follow"><meta property="og:type" content="article"><meta property="og:site_name" content="Projeto Vetor"><meta property="og:title" content="${escapeHtml(seoTitle)}"><meta property="og:description" content="${escapeHtml(description)}"><meta property="og:url" content="${canonical}"><meta name="twitter:card" content="summary"><meta name="twitter:title" content="${escapeHtml(seoTitle)}"><meta name="twitter:description" content="${escapeHtml(description)}"><link rel="icon" href="/brand/projeto-vetor-logo.svg"><script type="application/ld+json">${jsonForScript(jsonLd)}</script><script>window.MathJax={tex:{inlineMath:[["$","$"],["\\(","\\)"]],displayMath:[["$$","$$"],["\\[","\\]"]]}};</script><script defer src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-mml-chtml.js"></script>
<style>:root{font-family:Inter,ui-sans-serif,system-ui,sans-serif;color:#111827;background:#f1f5f9}*{box-sizing:border-box}body{margin:0}header{height:72px;background:#07122e;color:white;display:flex;align-items:center;justify-content:space-between;padding:0 max(20px,calc((100% - 1120px)/2))}header a{color:white;text-decoration:none;font-weight:800;display:flex;gap:10px;align-items:center}header img{width:38px;height:38px}nav{display:flex;gap:18px}main{max-width:900px;margin:36px auto;padding:0 18px}.card{background:white;border:1px solid #dbe3ef;border-radius:24px;padding:clamp(22px,4vw,42px);box-shadow:0 12px 35px #0f172a12}.eyebrow{color:#2563eb;font-weight:800}.meta,.tags{display:flex;gap:8px;flex-wrap:wrap;margin:16px 0}.tag{background:#eff6ff;color:#1d4ed8;border-radius:999px;padding:7px 11px;font-size:13px}.statement{font-size:18px;line-height:1.75;white-space:pre-wrap;overflow-x:auto}.statement img,.option img,.resolution img{display:block;max-width:100%;max-height:520px;object-fit:contain;margin:18px auto;border-radius:12px}.options{display:grid;gap:12px;margin-top:28px}.option{background:white;border:2px solid #dbe3ef;border-radius:16px;padding:17px;text-align:left;font:inherit;display:grid;grid-template-columns:36px 1fr;align-items:center;cursor:pointer}.option strong{color:#2563eb}.option:hover{border-color:#60a5fa}.option.correct{border-color:#16a34a;background:#f0fdf4}.option.wrong{border-color:#dc2626;background:#fef2f2}.feedback{font-weight:800;margin:18px 0}.resolution{display:none;margin-top:22px;border-top:1px solid #e2e8f0;padding-top:24px}.resolution.visible{display:block}.resolution-block{white-space:pre-wrap;line-height:1.7;margin:12px 0}.actions{display:flex;gap:10px;flex-wrap:wrap}.btn{display:inline-block;border:0;border-radius:12px;background:#2563eb;color:white;padding:13px 18px;font-weight:750;text-decoration:none;cursor:pointer}.btn.secondary{background:#e2e8f0;color:#172033}.about,.related{margin-top:26px}.about{background:#07122e;color:white;border-radius:22px;padding:26px}.related{display:grid;gap:10px}.related a{background:white;border:1px solid #dbe3ef;border-radius:15px;padding:16px;text-decoration:none;color:#172033;display:flex;flex-direction:column;gap:5px}.related span{color:#64748b}footer{text-align:center;color:#64748b;padding:42px 16px}@media(max-width:640px){nav a:first-child{display:none}.card{border-radius:18px}.option{grid-template-columns:30px 1fr;padding:14px}}</style></head><body><header><a href="/"><img src="/brand/projeto-vetor-logo.svg" alt="Projeto Vetor"><span>Projeto Vetor</span></a><nav><a href="/planos">Planos</a><a href="/login">Entrar</a></nav></header><main><article class="card"><p class="eyebrow">Questão pública comentada</p><h1>${escapeHtml(question.title)}</h1><div class="meta"><span>${escapeHtml(question.institution)}</span><span>•</span><span>${escapeHtml(question.year || "")}</span><span>•</span><span>${escapeHtml(question.difficulty || "Dificuldade não informada")}</span></div><div class="tags">${tags}</div><div class="statement">${escapeHtml(question.statement)}${question.imageUrl ? `<img src="${escapeHtml(question.imageUrl)}" alt="Imagem do enunciado" loading="eager">` : ""}${escapeHtml(question.statementAfterImage)}${question.formula ? `<div>${escapeHtml(question.formula)}</div>` : ""}</div><div class="options" aria-label="Alternativas">${options}</div><p id="feedback" class="feedback" role="status"></p><div class="actions"><button id="show-resolution" class="btn secondary" type="button">Ver resolução</button><button id="register-attempt" class="btn secondary" type="button" hidden>Registrar esta questão no meu histórico</button></div><section id="resolution" class="resolution"><h2>Resolução comentada</h2><div id="resolution-content"></div></section></article><section class="about"><h2>Estude com o Projeto Vetor</h2><p>Encontre questões, explicações didáticas e simuladores para organizar sua preparação.</p><div class="actions"><a class="btn" href="/plataforma/banco-questoes">Conheça o Banco de Questões</a><a class="btn secondary" href="/cadastro">Crie sua conta</a><a class="btn secondary" href="/planos">Conheça os planos</a></div></section>${related ? `<section class="related"><h2>Questões relacionadas</h2>${related}</section>` : ""}</main><footer>© Projeto Vetor · Plataforma de estudos</footer>
<script>(()=>{const slug=${jsonForScript(question.slug)},started=Date.now();let selected=null,answer=null;const buttons=[...document.querySelectorAll('.option')],feedback=document.getElementById('feedback'),resolution=document.getElementById('resolution'),content=document.getElementById('resolution-content'),show=document.getElementById('show-resolution'),register=document.getElementById('register-attempt');function token(){for(let i=0;i<localStorage.length;i++){const k=localStorage.key(i)||'';if(k.startsWith('sb-')&&k.endsWith('-auth-token')){try{return JSON.parse(localStorage.getItem(k)||'null')?.access_token||null}catch{}}}return null}function render(){if(!answer)return;buttons.forEach(b=>{b.disabled=true;const key=b.dataset.option;if(key===answer.correctOption)b.classList.add('correct');else if(key===selected)b.classList.add('wrong')});feedback.textContent=answer.isCorrect===null?'Gabarito e resolução exibidos.':answer.isCorrect?'Resposta correta!':'Resposta incorreta. A alternativa correta foi destacada.';content.replaceChildren();answer.resolution.forEach(block=>{const el=document.createElement('div');el.className='resolution-block';if(block.imageUrl){const img=document.createElement('img');img.src=block.imageUrl;img.alt='Imagem da resolução';el.append(img)}else{el.textContent=block.text||''}content.append(el)});if(window.MathJax?.typesetPromise)window.MathJax.typesetPromise([content]);resolution.classList.add('visible');show.hidden=true;register.hidden=!selected||!token()}async function submit(key){if(answer)return;selected=key;feedback.textContent='Verificando resposta…';try{const response=await fetch('/api/public-question/answer',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({slug,selectedOption:key})});if(!response.ok)throw new Error();answer=await response.json();render()}catch{feedback.textContent='Não foi possível verificar agora. Tente novamente.'}}buttons.forEach(b=>b.addEventListener('click',()=>submit(b.dataset.option)));show.addEventListener('click',async()=>{if(answer){render();return}feedback.textContent='Carregando resolução…';try{const response=await fetch('/api/public-question/answer',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({slug,reveal:true})});if(!response.ok)throw new Error();answer=await response.json();render()}catch{feedback.textContent='Não foi possível carregar agora. Tente novamente.'}});register.addEventListener('click',async()=>{const accessToken=token();if(!accessToken){location.href='/login';return}register.disabled=true;register.textContent='Registrando…';const response=await fetch('/api/public-question/register',{method:'POST',headers:{'content-type':'application/json','authorization':'Bearer '+accessToken},body:JSON.stringify({slug,selectedOption:selected,timeSpentSeconds:Math.max(1,Math.round((Date.now()-started)/1000))})});register.textContent=response.ok?'Registrada no seu histórico':'Não foi possível registrar';if(!response.ok)register.disabled=false})})();</script></body></html>`;
}

export function buildNotFoundHtml(): string {
  return `<!doctype html><html lang="pt-BR"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Conteúdo não disponível | Projeto Vetor</title><meta name="robots" content="noindex,nofollow"></head><body style="font-family:system-ui;text-align:center;padding:12vh 20px"><h1>Conteúdo não disponível</h1><p>Esta questão não existe ou não está disponível publicamente.</p><a href="https://www.projetovetor.com/">Voltar ao Projeto Vetor</a></body></html>`;
}
