import { useEffect, useMemo, useRef, useState } from "react";
import { Link, useRoute } from "wouter";
import { ArrowDown, ArrowLeft, ArrowRight, ArrowUp, Copy, Download, Eye, FileJson, History, Loader2, Plus, Save, Send, Trash2, Upload } from "lucide-react";
import AdminGuard from "@/components/admin/AdminGuard";
import AdminLayout from "@/components/admin/AdminLayout";
import { LessonRenderer } from "@/components/lessons/LessonRenderer";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { hasUnsavedLessonChanges, useUnsavedLessonChanges } from "@/hooks/useUnsavedLessonChanges";
import { trpc } from "@/lib/trpc";
import {
  LESSON_SCHEMA_VERSION,
  convertLessonV1ToV2,
  gridPresets,
  lessonJsonSchema,
  type GridBlock,
  type LessonBlock,
  type LessonJSON,
  type LessonMetadata,
  type SimpleLessonBlock,
} from "@shared/lessonSchema";

type Tab = "edit" | "preview" | "history";
const inputClass = "h-10 w-full rounded-xl border border-slate-200 bg-white px-3 text-sm text-slate-900 outline-none focus:border-cyan-500 dark:border-slate-700 dark:bg-slate-950 dark:text-slate-100";
const textareaClass = "min-h-28 w-full rounded-xl border border-slate-200 bg-white p-3 text-sm text-slate-900 outline-none focus:border-cyan-500 dark:border-slate-700 dark:bg-slate-950 dark:text-slate-100";
const simpleLabels: Record<SimpleLessonBlock["type"], string> = { text: "Texto", formula: "Fórmula", highlight: "Destaque", intuition: "Intuição", common_mistake: "Erro comum", example: "Exemplo", image: "Imagem", summary: "Resumo", formula_card: "FormulaCard", derivation: "Demonstração" };
const blockLabels: Record<LessonBlock["type"], string> = { ...simpleLabels, section: "Seção", grid: "Grid" };
const presetLabels: Record<GridBlock["preset"], string> = { one: "1 coluna", two: "2 colunas", three: "3 colunas", four: "4 colunas", one_third_two_thirds: "1/3 + 2/3", two_thirds_one_third: "2/3 + 1/3" };

function newId(type: string) { return `${type}-${crypto.randomUUID().slice(0, 8)}`; }
function makeSimple(type: SimpleLessonBlock["type"]): SimpleLessonBlock {
  const common = { id: newId(type), visible: true };
  if (type === "text") return { ...common, type, content: "Novo texto didático." };
  if (type === "formula") return { ...common, type, latex: "x = x_0 + vt", align: "center" };
  if (type === "highlight") return { ...common, type, variant: "info", content: "Informação importante." };
  if (type === "intuition") return { ...common, type, content: "Explique aqui a intuição do conceito." };
  if (type === "common_mistake") return { ...common, type, content: "Descreva o erro e como evitá-lo." };
  if (type === "example") return { ...common, type, problem: "Enunciado do exemplo.", solution: "Resolução passo a passo." };
  if (type === "image") return { ...common, type, storagePath: "caminho-da-imagem.webp", alt: "Descrição objetiva da imagem", align: "center", size: "large" };
  if (type === "formula_card") return { ...common, type, title: "Fórmula principal", latex: "v_m = \\frac{\\Delta s}{\\Delta t}", terms: [], observations: [], validityConditions: [], variant: "default" };
  if (type === "derivation") return { ...common, type, title: "Demonstração", initiallyOpen: false, steps: [{ id: newId("passo"), text: "Explique o primeiro passo." }] };
  return { ...common, type: "summary", points: ["Ponto principal da aula."] };
}
function makeGrid(): GridBlock { return { id: newId("grid"), type: "grid", visible: true, preset: "two", columns: [{ id: newId("coluna"), blocks: [] }, { id: newId("coluna"), blocks: [] }] }; }
function makeRoot(type: LessonBlock["type"]): LessonBlock {
  if (type === "section") return { id: newId("secao"), type, visible: true, title: "Nova seção", anchor: newId("secao"), blocks: [] };
  if (type === "grid") return makeGrid();
  return makeSimple(type);
}
function cloneBlock<T extends LessonBlock | SimpleLessonBlock>(block: T): T {
  const clone = structuredClone(block) as LessonBlock;
  const renewSimple = (simple: SimpleLessonBlock) => {
    simple.id = newId(simple.type);
    if (simple.type === "derivation") simple.steps.forEach(step => { step.id = newId("passo"); });
    if (simple.type === "formula_card" && simple.derivation) simple.derivation.steps.forEach(step => { step.id = newId("passo"); });
  };
  const renewGrid = (grid: GridBlock) => { grid.id = newId("grid"); grid.columns.forEach(column => { column.id = newId("coluna"); column.blocks.forEach(renewSimple); }); };
  if (clone.type === "section") { clone.id = newId("secao"); clone.anchor = newId("secao"); clone.blocks.forEach(child => child.type === "grid" ? renewGrid(child) : renewSimple(child)); }
  else if (clone.type === "grid") renewGrid(clone);
  else renewSimple(clone);
  return clone as T;
}
function lines(value: string) { return value.split("\n").map(item => item.trim()).filter(Boolean); }

type DerivationData = Extract<SimpleLessonBlock, { type: "derivation" }> | NonNullable<Extract<SimpleLessonBlock, { type: "formula_card" }>["derivation"]>;
function DerivationFields({ value, onChange }: { value: DerivationData; onChange: (value: DerivationData) => void }) {
  const updateStep = (index: number, patch: Partial<DerivationData["steps"][number]>) => onChange({ ...value, steps: value.steps.map((step, position) => position === index ? { ...step, ...patch } : step) });
  return <div className="space-y-3 rounded-xl border border-violet-200 p-4 dark:border-violet-900"><label className="space-y-1 text-xs font-semibold text-slate-500">Título<input className={inputClass} value={value.title} onChange={e => onChange({ ...value, title: e.target.value })} /></label><label className="space-y-1 text-xs font-semibold text-slate-500">Introdução opcional<textarea className={textareaClass} value={value.introduction ?? ""} onChange={e => onChange({ ...value, introduction: e.target.value || undefined })} /></label><label className="flex items-center gap-2 text-xs font-semibold text-slate-600 dark:text-slate-300"><input type="checkbox" checked={value.initiallyOpen} onChange={e => onChange({ ...value, initiallyOpen: e.target.checked })} />Iniciar aberta</label>{value.steps.map((step, index) => <div key={step.id} className="space-y-2 rounded-xl bg-slate-50 p-3 dark:bg-slate-950"><div className="flex items-center justify-between"><strong className="text-sm">Passo {index + 1}</strong><div className="flex gap-1"><Button size="icon" variant="ghost" aria-label="Mover passo para cima" disabled={index === 0} onClick={() => { const next=[...value.steps]; [next[index-1],next[index]]=[next[index],next[index-1]]; onChange({...value,steps:next}); }}><ArrowUp className="h-4 w-4" /></Button><Button size="icon" variant="ghost" aria-label="Mover passo para baixo" disabled={index === value.steps.length-1} onClick={() => { const next=[...value.steps]; [next[index+1],next[index]]=[next[index],next[index+1]]; onChange({...value,steps:next}); }}><ArrowDown className="h-4 w-4" /></Button><Button size="icon" variant="ghost" aria-label="Duplicar passo" onClick={() => onChange({...value,steps:[...value.steps.slice(0,index+1),{...step,id:newId("passo")},...value.steps.slice(index+1)]})}><Copy className="h-4 w-4" /></Button><Button size="icon" variant="ghost" aria-label="Remover passo" disabled={value.steps.length === 1} onClick={() => onChange({...value,steps:value.steps.filter((_,position)=>position!==index)})}><Trash2 className="h-4 w-4 text-red-500" /></Button></div></div><input className={inputClass} placeholder="Título opcional" value={step.title ?? ""} onChange={e => updateStep(index,{title:e.target.value||undefined})} /><textarea className={textareaClass} placeholder="Explicação Markdown" value={step.text ?? ""} onChange={e => updateStep(index,{text:e.target.value||undefined})} /><textarea className={textareaClass} placeholder="Fórmula LaTeX opcional" value={step.latex ?? ""} onChange={e => updateStep(index,{latex:e.target.value||undefined})} /><textarea className={textareaClass} placeholder="Observação opcional" value={step.observation ?? ""} onChange={e => updateStep(index,{observation:e.target.value||undefined})} /></div>)}<Button size="sm" variant="outline" onClick={() => onChange({...value,steps:[...value.steps,{id:newId("passo"),text:"Novo passo."}]})}><Plus className="mr-1 h-4 w-4" />Adicionar passo</Button><label className="space-y-1 text-xs font-semibold text-slate-500">Conclusão opcional<textarea className={textareaClass} value={value.conclusion ?? ""} onChange={e => onChange({ ...value, conclusion: e.target.value || undefined })} /></label></div>;
}

function SimpleFields({ block, onChange }: { block: SimpleLessonBlock; onChange: (block: SimpleLessonBlock) => void }) {
  const patch = (value: Partial<SimpleLessonBlock>) => onChange({ ...block, ...value } as SimpleLessonBlock);
  const title = "title" in block ? <label className="space-y-1 text-xs font-semibold text-slate-500">Título{block.type !== "derivation" && " opcional"}<input className={inputClass} value={block.title ?? ""} onChange={e => patch({ title: e.target.value || undefined } as Partial<SimpleLessonBlock>)} /></label> : null;
  if (block.type === "text" || block.type === "intuition" || block.type === "common_mistake") return <div className="space-y-3">{title}<label className="space-y-1 text-xs font-semibold text-slate-500">Conteúdo Markdown<textarea className={textareaClass} value={block.content} onChange={e => patch({ content: e.target.value })} /></label></div>;
  if (block.type === "formula") return <div className="grid gap-3"><label className="space-y-1 text-xs font-semibold text-slate-500">LaTeX<textarea className={textareaClass} value={block.latex} onChange={e => patch({ latex: e.target.value })} /></label><label className="space-y-1 text-xs font-semibold text-slate-500">Legenda<input className={inputClass} value={block.caption ?? ""} onChange={e => patch({ caption: e.target.value || undefined })} /></label><label className="space-y-1 text-xs font-semibold text-slate-500">Alinhamento<select className={inputClass} value={block.align} onChange={e => patch({ align: e.target.value as typeof block.align })}><option value="left">Esquerda</option><option value="center">Centro</option><option value="right">Direita</option></select></label></div>;
  if (block.type === "highlight") return <div className="space-y-3">{title}<label className="space-y-1 text-xs font-semibold text-slate-500">Variante<select className={inputClass} value={block.variant} onChange={e => patch({ variant: e.target.value as typeof block.variant })}><option value="info">Informação</option><option value="attention">Atenção</option><option value="important">Importante</option><option value="tip">Dica</option><option value="key_concept">Conceito-chave</option><option value="observation">Observação</option></select></label><label className="space-y-1 text-xs font-semibold text-slate-500">Conteúdo<textarea className={textareaClass} value={block.content} onChange={e => patch({ content: e.target.value })} /></label></div>;
  if (block.type === "example") return <div className="space-y-3"><label className="space-y-1 text-xs font-semibold text-slate-500">Problema<textarea className={textareaClass} value={block.problem} onChange={e => patch({ problem: e.target.value })} /></label><label className="space-y-1 text-xs font-semibold text-slate-500">Resolução<textarea className={textareaClass} value={block.solution} onChange={e => patch({ solution: e.target.value })} /></label><label className="space-y-1 text-xs font-semibold text-slate-500">Conclusão opcional<textarea className={textareaClass} value={block.conclusion ?? ""} onChange={e => patch({ conclusion: e.target.value || undefined })} /></label></div>;
  if (block.type === "image") return <div className="grid gap-3 md:grid-cols-2"><label className="space-y-1 text-xs font-semibold text-slate-500 md:col-span-2">Caminho no Storage<input className={inputClass} value={block.storagePath} onChange={e => patch({ storagePath: e.target.value })} /></label><label className="space-y-1 text-xs font-semibold text-slate-500 md:col-span-2">Texto alternativo<input className={inputClass} value={block.alt} onChange={e => patch({ alt: e.target.value })} /></label><label className="space-y-1 text-xs font-semibold text-slate-500 md:col-span-2">Legenda<input className={inputClass} value={block.caption ?? ""} onChange={e => patch({ caption: e.target.value || undefined })} /></label><label className="space-y-1 text-xs font-semibold text-slate-500">Alinhamento<select className={inputClass} value={block.align} onChange={e => patch({ align: e.target.value as typeof block.align })}><option value="left">Esquerda</option><option value="center">Centro</option><option value="right">Direita</option></select></label><label className="space-y-1 text-xs font-semibold text-slate-500">Tamanho<select className={inputClass} value={block.size} onChange={e => patch({ size: e.target.value as typeof block.size })}><option value="small">Pequeno</option><option value="medium">Médio</option><option value="large">Grande</option><option value="full">Largura total</option></select></label></div>;
  if (block.type === "summary") return <div className="space-y-3">{title}<label className="space-y-1 text-xs font-semibold text-slate-500">Pontos principais, um por linha<textarea className={textareaClass} value={block.points.join("\n")} onChange={e => patch({ points: e.target.value.split("\n") })} /></label></div>;
  if (block.type === "derivation") return <DerivationFields value={block} onChange={value => onChange({ ...value, id:block.id, type:"derivation", visible:block.visible })} />;
  const terms = block.terms.map(term => [term.symbol,term.meaning,term.unit ?? "",term.note ?? ""].join(" | ")).join("\n");
  return <div className="space-y-3">{title}<label className="space-y-1 text-xs font-semibold text-slate-500">Fórmula principal<textarea className={textareaClass} value={block.latex} onChange={e => patch({latex:e.target.value})} /></label><label className="space-y-1 text-xs font-semibold text-slate-500">Descrição opcional<textarea className={textareaClass} value={block.description ?? ""} onChange={e => patch({description:e.target.value||undefined})} /></label><label className="space-y-1 text-xs font-semibold text-slate-500">Termos: símbolo | significado | unidade | observação<textarea className={textareaClass} value={terms} onChange={e => patch({terms:lines(e.target.value).map(line => { const [symbol,meaning,unit,note]=line.split("|").map(value=>value.trim()); return {symbol,meaning,unit:unit||undefined,note:note||undefined}; })})} /></label><div className="grid gap-3 md:grid-cols-2"><label className="space-y-1 text-xs font-semibold text-slate-500">Unidade SI<input className={inputClass} value={block.siUnit ?? ""} onChange={e => patch({siUnit:e.target.value||undefined})} /></label><label className="space-y-1 text-xs font-semibold text-slate-500">Variante<select className={inputClass} value={block.variant} onChange={e => patch({variant:e.target.value as typeof block.variant})}><option value="default">Padrão</option><option value="primary">Principal</option><option value="success">Sucesso</option><option value="warning">Atenção</option></select></label></div><label className="space-y-1 text-xs font-semibold text-slate-500">Condições de validade, uma por linha<textarea className={textareaClass} value={block.validityConditions.join("\n")} onChange={e => patch({validityConditions:lines(e.target.value)})} /></label><label className="space-y-1 text-xs font-semibold text-slate-500">Observações, uma por linha<textarea className={textareaClass} value={block.observations.join("\n")} onChange={e => patch({observations:lines(e.target.value)})} /></label>{block.derivation ? <><DerivationFields value={block.derivation} onChange={derivation => patch({derivation})} /><Button size="sm" variant="outline" onClick={() => patch({derivation:undefined})}>Remover demonstração embutida</Button></> : <Button size="sm" variant="outline" onClick={() => patch({derivation:{title:"Demonstração",initiallyOpen:false,steps:[{id:newId("passo"),text:"Explique o primeiro passo."}]}})}>Adicionar demonstração embutida</Button>}</div>;
}

type CardActions = { onChange: (block: SimpleLessonBlock) => void; onMove: (direction: -1 | 1) => void; onDuplicate: () => void; onRemove: () => void; index: number; total: number; moveColumn?: (direction: -1 | 1) => void };
function SimpleCard({ block, onChange, onMove, onDuplicate, onRemove, index, total, moveColumn }: { block: SimpleLessonBlock } & CardActions) {
  return <Card className="border-slate-200 bg-white p-4 dark:border-slate-700 dark:bg-slate-900"><div className="mb-3 flex flex-wrap items-center justify-between gap-2"><div><span className="font-bold">{simpleLabels[block.type]}</span><code className="ml-2 text-xs text-slate-400">{block.id}</code></div><div className="flex items-center gap-1"><label className="mr-1 flex items-center gap-2 text-xs"><input type="checkbox" checked={block.visible} onChange={e => onChange({...block,visible:e.target.checked})} />Visível</label>{moveColumn && <><Button size="icon" variant="ghost" aria-label="Mover para coluna anterior" onClick={() => moveColumn(-1)}><ArrowLeft className="h-4 w-4" /></Button><Button size="icon" variant="ghost" aria-label="Mover para próxima coluna" onClick={() => moveColumn(1)}><ArrowRight className="h-4 w-4" /></Button></>}<Button size="icon" variant="ghost" aria-label="Mover para cima" disabled={index===0} onClick={()=>onMove(-1)}><ArrowUp className="h-4 w-4" /></Button><Button size="icon" variant="ghost" aria-label="Mover para baixo" disabled={index===total-1} onClick={()=>onMove(1)}><ArrowDown className="h-4 w-4" /></Button><Button size="icon" variant="ghost" aria-label="Duplicar bloco" onClick={onDuplicate}><Copy className="h-4 w-4" /></Button><Button size="icon" variant="ghost" aria-label="Remover bloco" onClick={onRemove}><Trash2 className="h-4 w-4 text-red-500" /></Button></div></div><SimpleFields block={block} onChange={onChange} /></Card>;
}

function GridEditor({ grid, onChange }: { grid: GridBlock; onChange: (grid: GridBlock) => void }) {
  const [addTypes, setAddTypes] = useState<Record<string,SimpleLessonBlock["type"]>>({});
  const setPreset = (preset: GridBlock["preset"]) => { const count={one:1,two:2,three:3,four:4,one_third_two_thirds:2,two_thirds_one_third:2}[preset]; const columns=grid.columns.slice(0,count).map(column=>({...column,blocks:[...column.blocks]})); while(columns.length<count) columns.push({id:newId("coluna"),blocks:[]}); if(grid.columns.length>count) grid.columns.slice(count).flatMap(column=>column.blocks).forEach(block=>columns[count-1].blocks.push(block)); onChange({...grid,preset,columns}); };
  const updateColumn = (columnIndex:number, blocks:SimpleLessonBlock[]) => onChange({...grid,columns:grid.columns.map((column,index)=>index===columnIndex?{...column,blocks}:column)});
  const transfer = (columnIndex:number, blockIndex:number, direction:-1|1) => { const target=columnIndex+direction; if(target<0||target>=grid.columns.length)return; const columns=grid.columns.map(column=>({...column,blocks:[...column.blocks]})); const [block]=columns[columnIndex].blocks.splice(blockIndex,1); columns[target].blocks.push(block); onChange({...grid,columns}); };
  return <div className="space-y-4"><label className="block space-y-1 text-xs font-semibold text-slate-500">Preset responsivo<select className={inputClass} value={grid.preset} onChange={e=>setPreset(e.target.value as GridBlock["preset"])}>{gridPresets.map(value=><option value={value} key={value}>{presetLabels[value]}</option>)}</select></label><div className="grid gap-4 xl:grid-cols-2">{grid.columns.map((column,columnIndex)=><div key={column.id} className="min-w-0 space-y-3 rounded-2xl border border-dashed border-slate-300 p-3 dark:border-slate-700"><div className="text-xs font-bold uppercase tracking-wide text-slate-500">Coluna {columnIndex+1} · <code>{column.id}</code></div>{column.blocks.map((block,index)=><SimpleCard key={block.id} block={block} index={index} total={column.blocks.length} onChange={value=>updateColumn(columnIndex,column.blocks.map((item,position)=>position===index?value:item))} onMove={direction=>{const target=index+direction;if(target<0||target>=column.blocks.length)return;const next=[...column.blocks];[next[index],next[target]]=[next[target],next[index]];updateColumn(columnIndex,next);}} onDuplicate={()=>updateColumn(columnIndex,[...column.blocks.slice(0,index+1),cloneBlock(block),...column.blocks.slice(index+1)])} onRemove={()=>window.confirm("Remover este bloco?")&&updateColumn(columnIndex,column.blocks.filter((_,position)=>position!==index))} moveColumn={direction=>transfer(columnIndex,index,direction)} />)}<div className="flex flex-wrap gap-2"><select className={`${inputClass} w-44`} value={addTypes[column.id]??"text"} onChange={e=>setAddTypes(current=>({...current,[column.id]:e.target.value as SimpleLessonBlock["type"]}))}>{Object.entries(simpleLabels).map(([value,label])=><option key={value} value={value}>{label}</option>)}</select><Button size="sm" variant="outline" onClick={()=>updateColumn(columnIndex,[...column.blocks,makeSimple(addTypes[column.id]??"text")])}><Plus className="mr-1 h-4 w-4" />Adicionar</Button></div></div>)}</div></div>;
}

function RootBlockCard({ block, index, total, onChange, onMove, onDuplicate, onRemove }: { block: LessonBlock; index:number; total:number; onChange:(block:LessonBlock)=>void; onMove:(direction:-1|1)=>void; onDuplicate:()=>void; onRemove:()=>void }) {
  const [childType,setChildType]=useState<SimpleLessonBlock["type"]|"grid">("text");
  if (block.type !== "section" && block.type !== "grid") return <SimpleCard block={block} index={index} total={total} onChange={onChange} onMove={onMove} onDuplicate={onDuplicate} onRemove={onRemove} />;
  const moveSectionChild = (childIndex:number,direction:-1|1) => { if(block.type!=="section")return;const target=childIndex+direction;if(target<0||target>=block.blocks.length)return;const next=[...block.blocks];[next[childIndex],next[target]]=[next[target],next[childIndex]];onChange({...block,blocks:next}); };
  return <Card className="border-2 border-cyan-200 bg-cyan-50/20 p-5 dark:border-cyan-900 dark:bg-cyan-950/10"><div className="mb-4 flex flex-wrap items-center justify-between gap-2"><div><span className="font-bold">{blockLabels[block.type]}</span><code className="ml-2 text-xs text-slate-400">{block.id}</code></div><div className="flex gap-1"><label className="mr-1 flex items-center gap-2 text-xs"><input type="checkbox" checked={block.visible} onChange={e=>onChange({...block,visible:e.target.checked})} />Visível</label><Button size="icon" variant="ghost" aria-label="Mover para cima" disabled={index===0} onClick={()=>onMove(-1)}><ArrowUp className="h-4 w-4" /></Button><Button size="icon" variant="ghost" aria-label="Mover para baixo" disabled={index===total-1} onClick={()=>onMove(1)}><ArrowDown className="h-4 w-4" /></Button><Button size="icon" variant="ghost" aria-label="Duplicar" onClick={onDuplicate}><Copy className="h-4 w-4" /></Button><Button size="icon" variant="ghost" aria-label="Remover" onClick={onRemove}><Trash2 className="h-4 w-4 text-red-500" /></Button></div></div>{block.type === "grid" ? <GridEditor grid={block} onChange={onChange} /> : <div className="space-y-4"><div className="grid gap-3 md:grid-cols-2"><label className="space-y-1 text-xs font-semibold text-slate-500">Título<input className={inputClass} value={block.title} onChange={e=>onChange({...block,title:e.target.value})} /></label><label className="space-y-1 text-xs font-semibold text-slate-500">Âncora<input className={inputClass} value={block.anchor} onChange={e=>onChange({...block,anchor:e.target.value})} /></label><label className="space-y-1 text-xs font-semibold text-slate-500 md:col-span-2">Descrição opcional<textarea className={textareaClass} value={block.description??""} onChange={e=>onChange({...block,description:e.target.value||undefined})} /></label></div>{block.blocks.map((child,childIndex)=>child.type==="grid"?<Card key={child.id} className="p-4"><div className="mb-3 flex justify-between"><strong>Grid interno</strong><div className="flex"><Button size="icon" variant="ghost" aria-label="Mover grid para cima" disabled={childIndex===0} onClick={()=>moveSectionChild(childIndex,-1)}><ArrowUp className="h-4 w-4" /></Button><Button size="icon" variant="ghost" aria-label="Mover grid para baixo" disabled={childIndex===block.blocks.length-1} onClick={()=>moveSectionChild(childIndex,1)}><ArrowDown className="h-4 w-4" /></Button><Button size="icon" variant="ghost" aria-label="Duplicar grid" onClick={()=>onChange({...block,blocks:[...block.blocks.slice(0,childIndex+1),cloneBlock(child),...block.blocks.slice(childIndex+1)]})}><Copy className="h-4 w-4" /></Button><Button size="icon" variant="ghost" aria-label="Remover grid" onClick={()=>window.confirm("Remover este grid?")&&onChange({...block,blocks:block.blocks.filter((_,position)=>position!==childIndex)})}><Trash2 className="h-4 w-4 text-red-500" /></Button></div></div><GridEditor grid={child} onChange={value=>onChange({...block,blocks:block.blocks.map((item,position)=>position===childIndex?value:item)})} /></Card>:<SimpleCard key={child.id} block={child} index={childIndex} total={block.blocks.length} onChange={value=>onChange({...block,blocks:block.blocks.map((item,position)=>position===childIndex?value:item)})} onMove={direction=>moveSectionChild(childIndex,direction)} onDuplicate={()=>onChange({...block,blocks:[...block.blocks.slice(0,childIndex+1),cloneBlock(child),...block.blocks.slice(childIndex+1)]})} onRemove={()=>window.confirm("Remover este bloco?")&&onChange({...block,blocks:block.blocks.filter((_,position)=>position!==childIndex)})} />)}<div className="flex flex-wrap gap-2"><select className={`${inputClass} w-48`} value={childType} onChange={e=>setChildType(e.target.value as typeof childType)}>{Object.entries(simpleLabels).map(([value,label])=><option value={value} key={value}>{label}</option>)}<option value="grid">Grid</option></select><Button size="sm" variant="outline" onClick={()=>onChange({...block,blocks:[...block.blocks,childType==="grid"?makeGrid():makeSimple(childType)]})}><Plus className="mr-1 h-4 w-4" />Adicionar à seção</Button></div></div>}</Card>;
}

function snapshot(metadata: LessonMetadata | null, document: LessonJSON) { return metadata ? JSON.stringify({metadata,document}) : ""; }

export default function AdminLessonEditorPage() {
  const [, params] = useRoute("/admin/aulas/:id");
  const lessonId = params?.id ?? "";
  const requestedTab = new URLSearchParams(window.location.search).get("tab");
  const [tab,setTab]=useState<Tab>(requestedTab==="preview"||requestedTab==="history"?requestedTab:"edit");
  const [metadata,setMetadata]=useState<LessonMetadata|null>(null);
  const [document,setDocument]=useState<LessonJSON>({schemaVersion:LESSON_SCHEMA_VERSION,blocks:[]});
  const [addType,setAddType]=useState<LessonBlock["type"]>("text");
  const [message,setMessage]=useState(""); const [issues,setIssues]=useState<Array<{path:string;message:string}>>([]); const [jsonText,setJsonText]=useState("");
  const initialized=useRef(false); const [savedSnapshot,setSavedSnapshot]=useState("");
  const detail=trpc.lessons.getAdmin.useQuery({lessonId},{enabled:Boolean(lessonId)}); const taxonomy=trpc.lessons.taxonomy.useQuery(); const history=trpc.lessons.history.useQuery({lessonId},{enabled:Boolean(lessonId)&&tab==="history"});
  const save=trpc.lessons.saveDraft.useMutation(); const validateDraft=trpc.lessons.validateDraft.useMutation(); const validateImport=trpc.lessons.validateImport.useMutation(); const publish=trpc.lessons.publish.useMutation(); const restore=trpc.lessons.restore.useMutation();
  useEffect(()=>{if(!detail.data||initialized.current)return;const lesson=detail.data.lesson as Record<string,unknown>;const parsed=lessonJsonSchema.safeParse(detail.data.draft.content_json);const nextMetadata={title:String(lesson.title),slug:String(lesson.slug),description:String(lesson.description),discipline:String(lesson.discipline),content:String(lesson.content),subject:String(lesson.subject),level:typeof lesson.level==="string"?lesson.level:null,displayOrder:Number(lesson.display_order??0)};setMetadata(nextMetadata);if(parsed.success){setDocument(parsed.data);setSavedSnapshot(snapshot(nextMetadata,parsed.data));}initialized.current=true;},[detail.data]);
  const currentSnapshot=useMemo(()=>snapshot(metadata,document),[metadata,document]); const dirty=hasUnsavedLessonChanges(savedSnapshot,currentSnapshot); useUnsavedLessonChanges(dirty);
  const selectedDiscipline=taxonomy.data?.find(item=>item.name===metadata?.discipline); const selectedContent=selectedDiscipline?.contents.find(item=>item.name===metadata?.content); const exportDocument=useMemo(()=>metadata?JSON.stringify({metadata,lesson:document},null,2):"",[metadata,document]);
  const updateRoot=(index:number,block:LessonBlock)=>setDocument(current=>({...current,blocks:current.blocks.map((item,position)=>position===index?block:item)} as LessonJSON));
  const moveRoot=(index:number,direction:-1|1)=>{const target=index+direction;if(target<0||target>=document.blocks.length)return;const blocks=[...document.blocks];[blocks[index],blocks[target]]=[blocks[target],blocks[index]];setDocument({...document,blocks} as LessonJSON);};
  async function saveDraft(){if(!metadata)return false;setMessage("");setIssues([]);try{const validation=await validateDraft.mutateAsync({metadata,content:document});if(!validation.valid){setIssues(validation.issues);setMessage("Corrija os campos indicados antes de salvar.");return false;}await save.mutateAsync({lessonId,metadata,content:document});setSavedSnapshot(snapshot(metadata,document));setMessage("Rascunho salvo. A versão publicada não foi alterada.");return true;}catch(error){setMessage(error instanceof Error?error.message:"Não foi possível salvar.");return false;}}
  async function publishDraft(){if(!window.confirm("Publicar este rascunho como uma nova versão imutável?" )||!metadata)return;const validation=await validateImport.mutateAsync({document:{metadata,lesson:document}});if(!validation.valid){setIssues(validation.issues);setMessage("Corrija os campos indicados antes de publicar.");return;}if(!await saveDraft())return;try{const result=await publish.mutateAsync({lessonId});setMessage(`Versão ${result.versionNumber} publicada com sucesso.`);await history.refetch();}catch(error){setMessage(error instanceof Error?error.message:"Não foi possível publicar.");}}
  function downloadJson(){const blob=new Blob([exportDocument],{type:"application/json"});const url=URL.createObjectURL(blob);const anchor=globalThis.document.createElement("a");anchor.href=url;anchor.download=`${metadata?.slug??"aula"}.json`;anchor.click();URL.revokeObjectURL(url);}
  async function checkImport(apply:boolean){setIssues([]);setMessage("");let value:unknown;try{value=JSON.parse(jsonText);}catch{setIssues([{path:"$",message:"JSON inválido."}]);return;}const result=await validateImport.mutateAsync({document:value});if(!result.valid||!result.document){setIssues(result.issues);return;}if(apply&&window.confirm("Substituir o rascunho aberto pelos dados validados? A alteração só será persistida ao salvar.")){setMetadata(result.document.metadata);setDocument(result.document.lesson);setAddType("text");setMessage("JSON aplicado localmente. Revise e salve o rascunho.");}else if(!apply)setMessage("JSON válido. Nenhum dado foi alterado.");}
  async function loadJsonFile(file:File|undefined){if(!file)return;if(file.size>1_000_000){setIssues([{path:"$",message:"O arquivo JSON excede 1 MB."}]);return;}setJsonText(await file.text());setMessage("Arquivo carregado localmente. Valide antes de importar.");}
  async function restoreVersion(versionId:string){if(!window.confirm("Copiar esta versão para o rascunho atual? O histórico publicado será preservado."))return;await restore.mutateAsync({lessonId,versionId});initialized.current=false;setSavedSnapshot("");await detail.refetch();setTab("preview");setMessage("Versão copiada para o rascunho. Publique novamente quando estiver pronta.");}
  if(detail.error)return <AdminGuard allowedRoles={["admin"]}><div className="p-10 text-red-600">{detail.error.message}</div></AdminGuard>;
  if(detail.isLoading||!metadata)return <AdminGuard allowedRoles={["admin"]}><div className="flex min-h-screen items-center justify-center bg-slate-50 dark:bg-slate-950"><Loader2 className="h-7 w-7 animate-spin" /></div></AdminGuard>;
  return <AdminGuard allowedRoles={["admin"]}><AdminLayout title={metadata.title} subtitle={`Rascunho schema v${document.schemaVersion}, isolado da versão publicada.`}>
    <div className="flex flex-wrap items-center justify-between gap-3"><Link href="/admin/aulas"><Button variant="outline"><ArrowLeft className="mr-2 h-4 w-4" />Aulas</Button></Link><div className="flex flex-wrap gap-2">{document.schemaVersion===1&&<Button variant="outline" onClick={()=>window.confirm("Converter este rascunho para schema v2? O conteúdo existente será preservado e só muda no banco depois de salvar.")&&setDocument(convertLessonV1ToV2(document))}>Converter para v2</Button>}<Button variant="outline" onClick={downloadJson}><Download className="mr-2 h-4 w-4" />Exportar JSON</Button><Button variant="outline" onClick={saveDraft} disabled={save.isPending}><Save className="mr-2 h-4 w-4" />Salvar rascunho{dirty?" *":""}</Button><Button onClick={publishDraft} disabled={publish.isPending}><Send className="mr-2 h-4 w-4" />Publicar</Button></div></div>
    {dirty&&<div className="rounded-xl border border-amber-300 bg-amber-50 p-3 text-sm font-semibold text-amber-900 dark:border-amber-800 dark:bg-amber-950/40 dark:text-amber-100">Alterações locais ainda não salvas.</div>}{message&&<div aria-live="polite" className="rounded-xl border border-cyan-200 bg-cyan-50 p-3 text-sm text-cyan-900 dark:border-cyan-900 dark:bg-cyan-950/40 dark:text-cyan-100">{message}</div>}{issues.length>0&&<div role="alert" className="rounded-xl border border-red-200 bg-red-50 p-4 dark:border-red-900 dark:bg-red-950/40"><p className="mb-2 font-bold text-red-800 dark:text-red-200">Corrija os campos abaixo:</p><ul className="space-y-1 text-sm text-red-700 dark:text-red-300">{issues.map((issue,index)=><li key={index}><code>{issue.path}</code>: {issue.message}</li>)}</ul></div>}
    <div className="flex gap-2 border-b border-slate-200 dark:border-slate-700">{(["edit","preview","history"] as const).map(value=><button key={value} onClick={()=>setTab(value)} className={`border-b-2 px-4 py-3 text-sm font-semibold ${tab===value?"border-cyan-500 text-cyan-700 dark:text-cyan-300":"border-transparent text-slate-500"}`}>{value==="edit"?"Editar":value==="preview"?"Preview":"Histórico"}</button>)}</div>
    {tab==="edit"&&<div className="grid gap-6 xl:grid-cols-[minmax(0,1fr)_320px]"><div className="space-y-4">{document.blocks.map((block,index)=><RootBlockCard key={block.id} block={block} index={index} total={document.blocks.length} onChange={value=>updateRoot(index,value)} onMove={direction=>moveRoot(index,direction)} onDuplicate={()=>setDocument({...document,blocks:[...document.blocks.slice(0,index+1),cloneBlock(block),...document.blocks.slice(index+1)]} as LessonJSON)} onRemove={()=>window.confirm("Remover este bloco do rascunho?")&&setDocument({...document,blocks:document.blocks.filter((_,position)=>position!==index)} as LessonJSON)} />)}<Card className="flex flex-wrap gap-2 border-dashed p-4"><select className={`${inputClass} w-52`} value={addType} onChange={e=>setAddType(e.target.value as LessonBlock["type"])}>{Object.entries(blockLabels).filter(([value])=>document.schemaVersion===2||!['section','grid','formula_card','derivation'].includes(value)).map(([value,label])=><option value={value} key={value}>{label}</option>)}</select><Button onClick={()=>setDocument({...document,blocks:[...document.blocks,makeRoot(addType)]} as LessonJSON)}><Plus className="mr-2 h-4 w-4" />Adicionar bloco</Button></Card></div>
      <div className="space-y-4"><Card className="space-y-3 border-slate-200 bg-white p-4 dark:border-slate-700 dark:bg-slate-900"><h3 className="font-bold">Metadados</h3><label className="block text-xs font-semibold text-slate-500">Título<input className={inputClass} value={metadata.title} onChange={e=>setMetadata({...metadata,title:e.target.value})} /></label><label className="block text-xs font-semibold text-slate-500">Slug<input className={inputClass} value={metadata.slug} onChange={e=>setMetadata({...metadata,slug:e.target.value})} /></label><label className="block text-xs font-semibold text-slate-500">Descrição<textarea className={textareaClass} value={metadata.description} onChange={e=>setMetadata({...metadata,description:e.target.value})} /></label><label className="block text-xs font-semibold text-slate-500">Disciplina<select className={inputClass} value={metadata.discipline} onChange={e=>setMetadata({...metadata,discipline:e.target.value,content:"",subject:""})}>{taxonomy.data?.map(item=><option key={item.name}>{item.name}</option>)}</select></label><label className="block text-xs font-semibold text-slate-500">Conteúdo<select className={inputClass} value={metadata.content} onChange={e=>setMetadata({...metadata,content:e.target.value,subject:""})}>{selectedDiscipline?.contents.map(item=><option key={item.name}>{item.name}</option>)}</select></label><label className="block text-xs font-semibold text-slate-500">Assunto<select className={inputClass} value={metadata.subject} onChange={e=>setMetadata({...metadata,subject:e.target.value})}>{selectedContent?.subjects.map(value=><option key={value}>{value}</option>)}</select></label><label className="block text-xs font-semibold text-slate-500">Nível<input className={inputClass} value={metadata.level??""} onChange={e=>setMetadata({...metadata,level:e.target.value||null})} /></label></Card><Card className="space-y-3 border-slate-200 bg-white p-4 dark:border-slate-700 dark:bg-slate-900"><h3 className="flex items-center gap-2 font-bold"><FileJson className="h-4 w-4" />Importar JSON</h3><input type="file" accept="application/json,.json" aria-label="Selecionar arquivo JSON" className="block w-full text-xs text-slate-500" onChange={e=>loadJsonFile(e.target.files?.[0])} /><textarea className={`${textareaClass} min-h-48 font-mono text-xs`} value={jsonText} onChange={e=>setJsonText(e.target.value)} placeholder="Cole JSON v1 ou v2" /><div className="flex flex-wrap gap-2"><Button size="sm" variant="outline" onClick={()=>checkImport(false)}><Eye className="mr-1 h-4 w-4" />Validar sem importar</Button><Button size="sm" variant="outline" onClick={()=>checkImport(true)}><Upload className="mr-1 h-4 w-4" />Importar no rascunho</Button></div></Card></div></div>}
    {tab==="preview"&&<div><div className="mb-5 rounded-xl border border-amber-300 bg-amber-50 p-3 text-center text-sm font-semibold text-amber-900 dark:border-amber-800 dark:bg-amber-950/40 dark:text-amber-100">Preview do rascunho — alunos continuam vendo somente a última versão publicada.</div><article className="mx-auto max-w-4xl rounded-3xl border border-slate-200 bg-white p-6 shadow-sm dark:border-slate-700 dark:bg-slate-900 md:p-10"><h1 className="text-3xl font-black text-slate-950 dark:text-white">{metadata.title}</h1><p className="mt-3 text-slate-600 dark:text-slate-300">{metadata.description}</p><p className="mb-8 mt-3 text-sm text-cyan-700 dark:text-cyan-300">{metadata.discipline} · {metadata.content} · {metadata.subject}</p><LessonRenderer lesson={document} mode="preview" /></article></div>}
    {tab==="history"&&<div className="space-y-3">{history.isLoading?<Loader2 className="h-6 w-6 animate-spin"/>:!history.data?.length?<Card className="p-8 text-center text-slate-500">Nenhuma versão publicada.</Card>:history.data.map(version=><Card key={version.id} className="flex flex-col gap-3 p-5 sm:flex-row sm:items-center sm:justify-between"><div><h3 className="font-bold">Versão {version.version_number} · schema v{version.schema_version}</h3><p className="text-sm text-slate-500">Publicada em {new Date(version.published_at).toLocaleString("pt-BR")}</p>{version.change_note&&<p className="mt-1 text-sm">{version.change_note}</p>}</div><Button variant="outline" onClick={()=>restoreVersion(version.id)} disabled={restore.isPending}><History className="mr-2 h-4 w-4" />Restaurar no rascunho</Button></Card>)}</div>}
  </AdminLayout></AdminGuard>;
}
