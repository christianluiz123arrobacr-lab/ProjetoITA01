// Compatibility facade: the canonical VET engine lives in shared/vet and is
// executed by the backend. Client pages import only types/formatters from here.
export * from "../../../shared/vet/vetEngine";
